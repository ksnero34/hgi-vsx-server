apiVersion: v1
kind: Service
metadata:
  name: {{ include "hgi-vsx.fullname" . }}-server
  labels:
    {{- include "hgi-vsx.labels" . | nindent 4 }}
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.server.service.port }}
      targetPort: http
  selector:
    {{- include "hgi-vsx.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: server
